<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ecp.dao.CustomerLevelRuleMapper" >
  <resultMap id="BaseResultMap" type="com.ecp.entity.CustomerLevelRule" >
    <!--
      WARNING - @mbggenerated
    -->
    <id column="id" property="id" jdbcType="BIGINT" />
    <result column="customer_type_id" property="customerTypeId" jdbcType="BIGINT" />
    <result column="customer_level_id" property="customerLevelId" jdbcType="BIGINT" />
    <result column="rule_type" property="ruleType" jdbcType="TINYINT" />
    <result column="rule_name" property="ruleName" jdbcType="VARCHAR" />
    <result column="comment" property="comment" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    <!--
      WARNING - @mbggenerated
    -->
    id, customer_type_id, customer_level_id, rule_type, rule_name, comment
  </sql>
  <!-- 根据条件（map中的参数）查询商品 -->
	<select id="getListMap" parameterType="map" resultType="map">
		SELECT
			lr.*, t.id AS type_id,
			t.type_name,
			t.type_code,
			l.id AS level_id,
			l.level_name,
			l.level_code
		FROM
			customer_level_rule lr
		LEFT JOIN customer_type t ON lr.customer_type_id = t.id
		LEFT JOIN customer_level l ON lr.customer_level_id = l.id
		ORDER BY
			lr.id DESC
	</select>
</mapper>